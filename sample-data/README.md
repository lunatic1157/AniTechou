# 示例数据（可移植备份）

此目录包含用于演示/验收的 AniTechou 可移植备份文件（`*.zip`）。

## 导入方式
1. 启动 AniTechou，创建一个新的测试账号
2. 打开「设置」页 -> 「导入数据」
3. 选择本目录内的 `*.zip` 备份文件进行导入

## 说明
- 该备份包含：作品数据、笔记内容、封面、笔记插图
- 导入会将资源解包到本机 `%LocalAppData%\AniTechou` 目录下

