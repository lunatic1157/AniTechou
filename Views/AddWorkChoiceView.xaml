<UserControl x:Class="AniTechou.Views.AddWorkChoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    
    <UserControl.Resources>
        <Style x:Key="ChoiceButton" TargetType="Button">
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="150"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#F0E9DD"/>
            <Setter Property="Foreground" Value="#5C4E3D"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="20"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0D5C0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="选择添加方式" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="#5C4E3D"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="AIAddButton" 
                        Style="{StaticResource ChoiceButton}"
                        Click="AIAdd_Click">
                    <StackPanel>
                        <TextBlock Text="🤖" FontSize="48" Margin="0,0,0,10"/>
                        <TextBlock Text="AI添加" FontSize="18"/>
                        <TextBlock Text="让AI帮你搜索并填充" FontSize="11" Foreground="#7F6E5A"/>
                    </StackPanel>
                </Button>
                
                <Button x:Name="ManualAddButton" 
                        Style="{StaticResource ChoiceButton}"
                        Click="ManualAdd_Click">
                    <StackPanel>
                        <TextBlock Text="✍️" FontSize="48" Margin="0,0,0,10"/>
                        <TextBlock Text="手动添加" FontSize="18"/>
                        <TextBlock Text="自己填写作品信息" FontSize="11" Foreground="#7F6E5A"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>