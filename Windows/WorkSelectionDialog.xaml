<Window x:Class="AniTechou.Windows.WorkSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择关联作品"
        Width="460" Height="560"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}">
    
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="选择要关联的作品" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>

        <TextBox x:Name="SearchBox"
                 Grid.Row="1"
                 Height="40"
                 Margin="0,0,0,12"
                 Style="{StaticResource AppTextBoxStyle}"
                 TextChanged="SearchBox_TextChanged"/>
        
        <ListBox x:Name="WorksList"
                 Grid.Row="2"
                 Margin="0,0,0,12"
                 SelectionMode="Multiple"
                 DisplayMemberPath="Title"
                 Background="{DynamicResource Surface1Brush}"
                 Foreground="{DynamicResource TextPrimaryBrush}"
                 BorderBrush="{DynamicResource BorderBrush}"
                 BorderThickness="1"/>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4,0,0">
            <Button Content="确定" Width="88" Height="36" Style="{StaticResource AppPrimaryButtonStyle}" Click="OK_Click" Margin="0,0,10,0"/>
            <Button Content="取消" Width="88" Height="36" Style="{StaticResource AppSecondaryButtonStyle}" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
